Пропущен код 6894011 (ООО "Браво авто"): недопустимая длина 7 знаков

class ContractItem(models.Model):
    """Спецификация договора (Строки списания лимитов)"""
    contract = models.ForeignKey(Contract, on_delete=models.CASCADE, related_name='items')

    year = models.PositiveIntegerField("Год позиции договора", db_index=True)

    # ЖЕСТКАЯ СВЯЗЬ С ДОЛЕЙ ПОДРАЗДЕЛЕНИЯ ИЗ ПЛАНА!
    plan_share = models.ForeignKey(PlanShare, on_delete=models.PROTECT, related_name='contract_items')

    # Списание конкретно под это подразделение
    contract_amount = models.DecimalField("Количество по договору", max_digits=12,
                                          decimal_places=3)  # 100 для ГУСЭ / 53 для ГУСМЭ
    contract_cost = models.DecimalField("Сумма по договору", max_digits=15,
                                        decimal_places=2)

Предусматривает ли данная модель и иные модели следующий реальный случай:
есть позиция плана - соляная кислота, количество - 100кг, ориентировочная стоимость - 10000BYN. Из них: 70кг - 7000BYN для управления 1,
30кг - 3000BYN для управления 2. Заключен договор на поставку соляной кислоты №4 от 01.06.2026, количество 100 кг на сумму 5000BYN, из них: 70кг - 3500BYN для управления 1,
30кг - 1500BYN для управления 2. Важно учитывать по каждой позиции плана закупок распределение сумм и объемов закупок между подразделений и фактическое такое распределение при внесении сведений о заключенном договоре.
Это помогает отслеживать остатки по выделенным для каждого подразделения денежных средств, оперативно вносить изменения в план закупок (увеличивать или уменьшать суммы).



